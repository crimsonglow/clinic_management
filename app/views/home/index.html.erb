<div class="card sadow">
  <h1>Welcome to the health center</h1>

  <% if current_user.patient? %>
    <%= link_to "personal profile", patient_path(current_user.id) %>
  <% elsif current_user.doctor? %>
    <%= link_to "personal profile", doctor_path(current_user.id) %>
   <% end %>
</div>
